            GameState->MossBlockBitmap = LoadBitmap(Memory, 0, "moss_block1.bmp");
            GameState->MagicianHandBitmap = LoadBitmap(Memory, 0, "magician_hand.bmp");
            GameState->Smoke = LoadBitmap(Memory, 0, "leaves.bmp");
            GameState->Shadow = LoadBitmap(Memory, 0, "Faroshadow.bmp");
            GameState->Fog = LoadBitmap(Memory, 0, "fog.bmp");
            GameState->FootPrint = LoadBitmap(Memory, 0, "footprint.bmp");
            GameState->DesertGround = LoadBitmap(Memory, 0, "desert.bmp");
            GameState->Man = LoadBitmap(Memory, 0, "headandshoulders.bmp");
            GameState->Shadow = LoadBitmap(Memory, 0, "faroshadow.bmp");
            GameState->Legs = LoadBitmap(Memory, 0, "legs.bmp");
            GameState->Arms = LoadBitmap(Memory, 0, "arms.bmp");
            